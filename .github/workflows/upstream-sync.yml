name: Upstream Sync

on:
  schedule:
    # Daily at 02:45 UTC, ahead of nightly build.
    - cron: '45 2 * * *'
  workflow_dispatch:

concurrency:
  group: upstream-sync
  cancel-in-progress: false

jobs:
  sync:
    name: Merge upstream into edge
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      actions: write
    steps:
      - name: Checkout edge branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: edge

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Fetch upstream
        env:
          UPSTREAM_REPO: https://github.com/has207/xenia-edge.git
          UPSTREAM_BRANCH: edge
        run: |
          git remote add upstream "$UPSTREAM_REPO" 2>/dev/null || git remote set-url upstream "$UPSTREAM_REPO"
          git fetch upstream "$UPSTREAM_BRANCH"

      - name: Merge upstream branch
        env:
          UPSTREAM_BRANCH: edge
        run: |
          set -e
          before_sha=$(git rev-parse HEAD)
          if git merge --no-ff --no-edit "upstream/${UPSTREAM_BRANCH}"; then
            after_sha=$(git rev-parse HEAD)
            if [ "$before_sha" = "$after_sha" ]; then
              echo "created_commit=false" >> "$GITHUB_ENV"
            else
              echo "created_commit=true" >> "$GITHUB_ENV"
            fi
          else
            echo "::error::Automatic upstream merge failed due to conflicts. Resolve manually."
            exit 1
          fi

      - name: Push to edge
        run: |
          if [ "${created_commit}" != "true" ]; then
            echo "No upstream changes to push."
            exit 0
          fi
          git push origin HEAD:edge

      - name: Trigger nightly workflow
        if: env.created_commit == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: gh workflow run nightly.yml --ref edge --repo "$GH_REPO"
